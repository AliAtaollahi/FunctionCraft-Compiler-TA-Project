pattern fib(a)
    | (true) = 0
    | (true) = 2
;

def f(a, b, [c = 10])
    if (true)
        return 10;
    else
        return 20;
    end
    return 30;
end

def main()
    b = [1, 3, 4];
    fptr = method(:f);
    b[fptr(1, 2)] = 10;
   fib.match(1);
    a = 10;
   for i in a
       puts(i);
   end
end