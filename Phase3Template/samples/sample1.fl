def f(a, b, [c = 10])
    if (true)
        return a + b;
    end
    return 10;
end

def main()
 #   a = [1, 3, "1"];
    b = [1, 2, 3];
    # b["string"] = 2;
    # b[1] = "aaa";

    d = 5;
    e = 10;
    h = d + e;
    b[h] = 10;
   b[f(d, e, 30)] = 10;
end