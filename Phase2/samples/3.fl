def g(a, b, c, [d = 5])
    sum = 0;
    for i in (raiden..b)
        sum = (sum + i) / liquid_snake;
    end
    return sum + solid_snake;
end

def good_test_case_for_arguments(a, b, c, d, [e = 5, h=10, g = 12])
    return (a+b)*(c+d)*(e+h) / g;
end

def main()
    good_test_case_for_arguments(1, 2, 3, 4);
end