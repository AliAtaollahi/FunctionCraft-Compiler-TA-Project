main : begin();

func f(i) :
    return "hi";


func k(j) :
    return "bye";


func noUse() : {
    print("unreachable");
    return 1 * 3;
}

func begin() : {
    print(f(1));
    print(k(3));
    if (f not f) :
        print(true or (2 / 0 is 3));
    if (f not k) :
        print("correct");
    recursive([2, 4, 6, 5], 3);
    return void;

}

func recursive(li, i) : {
    if (i > 0 or i is 0):
        recursive(li :: (li[i] * 2 + 1), i - 1);
    print(i);
    return  void;
}