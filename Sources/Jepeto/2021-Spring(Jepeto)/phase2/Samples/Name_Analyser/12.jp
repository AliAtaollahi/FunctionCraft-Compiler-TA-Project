main : print(a);


func f(d) : {
    return a + b * c;
    return a * a;
    if (d and dd) :
        print(d + 3 + b);

}

func g(a) : {
    if (a) :
        return b;
    else :
        return a * x;
}

