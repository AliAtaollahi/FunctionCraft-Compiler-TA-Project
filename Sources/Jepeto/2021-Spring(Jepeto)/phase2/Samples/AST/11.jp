func f(a, b, c, a1):
{
    if (g().size > f()):
        print(a1);
    else:
        if (a and b or c):{
            return 780;
        }
        print("PLC");
        return f() + g(true) / h(1,2, "pLc");
    return void;
}

func g():
    return 1;

func h(a, x, z):
    return 0;

main: f(a = 1, b = 2, c = 3);
