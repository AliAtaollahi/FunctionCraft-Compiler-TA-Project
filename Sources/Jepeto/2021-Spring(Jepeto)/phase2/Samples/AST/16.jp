func f9(a, aa, aaa, b, c, d):{
    if (a > b):{
        return a::aa[1].size;
    }
    else:{
        print(((a::b::c)[0]).size);
    }

    return f([], [1], [1, []], [1, "hi"], (a + b * c / a)[a and b not c - ~d]);
}

func f():
    return f9;

main: print([]);