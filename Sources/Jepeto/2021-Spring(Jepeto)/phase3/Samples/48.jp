main : listSize();


func listSize() : {
    return [[1, 2, 3], []];
    A();
    B();
}

func A() : {
    return listSize()[0].size;
    return listSize()[0][0].size;
    return listSize.size;
}

func B() : {
    return [true][0].size;
}