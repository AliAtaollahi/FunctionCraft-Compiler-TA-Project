func f(a, b, c):
{
    if (g().size > f(c = 1, b = 2, a = 5)):
        print(a);
    else:
        if (a and b or c):{
            return 780;
        }
        print("PLC");
        return f(3, 4, 5) + g() / h(1,2, "pLc");
    return void;
}

func g():
    return 1;

func h(a, x, z):
    return 0;

main: f(a = 1, b = 2, c = 3);
