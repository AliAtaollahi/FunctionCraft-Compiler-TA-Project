main: g(4);

func g(a): {
    if ([1,2,3]::2.size > 5):
        return [1, ["hi", "bye"], [[1,2], [true, false], ["PlC"]]];
    else :
        return a::2.size;
    create2dList([], 0, 0);
}



func create2dList(l, i, j) : {
    if i not 10 :
        return create2dList(l[i]::i::j * 2, i + 1, j + 1);
    else :
        printList(l, i);
    return void;
}

func printList(l, i) : {
    print(l[i][0]);
    print(l[i][1]);
    return printList(l, i - 1);
    return void;
}