main : f(b = true, a = 1);

func f(a, b) : {
    if (a) :
        return a;
    else : {
        if (b and [[2, 3, 4]][0].size > a) :
            g(k);
    }
    return a and a;
}

func g(a) : {
    return a()([true, a is false]);
}

func k() : {
    return last;
}

func last(a) : {
    if (a[0] > 4) :
        return a[0];
    else :
        return a[1];
}