def printList(l, i)
    puts(l[i][0]);
    puts(l[i][1]);
    printList(l, i - 1);
end

def create2dList(l, i, j)
    if(i is not 10)
        create2dList(l[i] << i << j * 2, i + 1, j + 1);
    else
        printList(l, i);
    end
end

def main()
    create2dList([], 0, 0);
end