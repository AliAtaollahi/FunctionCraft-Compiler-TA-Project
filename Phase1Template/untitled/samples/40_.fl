def doFunc(arg1, arg2, fptr)
    return fptr(arg1, arg2);
end

def main()
    doFunc("Mammad", 
           "Nemati", 
            -> (firstName, lastName) {return "Hi I'm " << firstName << lastName << ".";});
end
