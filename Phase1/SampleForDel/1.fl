def func()
    if (a > b)
        if (b > c)
            puts(a);
            return a;
        else
            return c;
        end
    else
        if (c > b)
            return c;
        else
            return b;
        end
    end
end

def compare(a, b)
    if (a > b)
        return true;
    else
        return false;
    end
end

def add(a, b)
    return a + b;
end

def main()
    c = a << b;
    push(a, c);
    puts(add(d, e));
    puts(len(b));
end