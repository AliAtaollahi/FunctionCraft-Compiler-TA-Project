def lambdaIndex(list)
    a = list[-> (c) {return 20 * c;}];
    return a + list[-> (b) {return 10 - b;}];
end

#Just a comment
#Hello

def exprFunc(x, y)
    a = b + c * f / h;
    b = a - y + h * j / l;
    return a + b - x + y;
end

def func()
    cnt = 1;
    loop do
        cnt = cnt + 1;
        break if (cnt > 100);
    end
    return cnt;
end

def main()
    list = [1, 2, 3];
    list = list << [1, 2, 3];
    return list[10];
end
