func f9(a, aa, aaa):{
    if (a > b):{
        return a::aa[1].size;
    }
    else:{
        puts(((a::b::c)[0]).size);
    }

    return f([], [1], [1, []], [1, "hi"], (a + b * c / a)[a and b not c - ~d]);
}

main: puts([]);