def main_ (a1, a2)
    if (a1 + a2)
        if (a1 - a2)
            puts(a1 - a2);
        else
            return;
            puts(a2);
        end
    else
        puts(a1);
    end
    return;
end

def main()
    puts(main_());
end